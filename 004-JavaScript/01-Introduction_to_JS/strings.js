// String Concatenation
var s = "Hello " + "World";
console.log(s);

// String length
console.log(s.length);

// Slicing and extracting parts of a string
var slice = s.slice(2, 11); // llo Wo
console.log(slice);

// Changing casing
var n = "Aryan";
console.log(n.toUpperCase());
console.log(n.toLowerCase());